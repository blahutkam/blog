import { createMuiTheme } from "@material-ui/core/styles";

const theme = createMuiTheme({
  pallete: {
    primary: {
      main: "#764abc",
    },
  },
  background: {
    default: "#fff",
  },
  body: {
    margin: "0px",
  },
});

export default theme;
